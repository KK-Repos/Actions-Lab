name: Reuseable-Workflow
on:
  workflow_call:
    inputs:
      tag_name:
        type: string
        required: true

jobs:
  unit-test:
    runs-on: ubuntu-latest
    env:
      tag: ${{inputs.tag_name}}
    steps:
      - name: print the tag name
        run: echo ${{inputs.tag_name}}
      - name: Checkout Branch
        uses: actions/checkout@v3
        with:
          persist-credentials: false
      - name: Unit test run
        run: docker run --entrypoint npm kk-ms:latest run test
          