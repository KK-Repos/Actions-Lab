name: test-checkout

on: workflow_dispatch

jobs:

  test-job:
    runs-on: ubuntu-latest
    name: test-checkout-job
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: print the dir 
        run: ls
            
        
