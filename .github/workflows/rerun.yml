name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    runs-on: ubuntu-latest
    outputs:
      runner: ${{ github.run_id }}
    steps:
    - id: runner
      run: echo test

  test:
    needs: [build]
    runs-on: ${{ needs.build.outputs.runner }}
    steps:
      - run: echo I Ran
